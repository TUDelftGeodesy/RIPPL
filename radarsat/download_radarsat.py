# This file contains a function to check which files for radarsat are available, which ones are downloaded and a quality
# check for the files which are downloaded.


def radarsat_available(shape=''):

    print 'lets add some code'

def radarsat_download(''):
    
    
def radarsat_quality_check(''):